@model IEnumerable<VnBookLibrary.Model.Entities.GroupRole>

@{
    /**/

    ViewBag.Title = "Index";
    Layout = "~/Areas/Manage/Views/Shared/_LayoutManage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GroupRoleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupRoleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.GroupRoleId }) |
                @Html.ActionLink("Details", "Details", new { id = item.GroupRoleId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.GroupRoleId })
            </td>
        </tr>
    }

</table>
